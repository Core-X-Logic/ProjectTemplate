Entity com.mycompanyname.zero.audit.domain.AuditLog lives in package com.mycompanyname.zero.audit.domain which has no package-info.java — Modulith's verify() passes an undeclared package by default, so this boundary is currently unchecked rather than checked and clean.
Entity com.mycompanyname.zero.audit.domain.EntityChange lives in package com.mycompanyname.zero.audit.domain which has no package-info.java — Modulith's verify() passes an undeclared package by default, so this boundary is currently unchecked rather than checked and clean.
Entity com.mycompanyname.zero.audit.domain.EntityPropertyChange lives in package com.mycompanyname.zero.audit.domain which has no package-info.java — Modulith's verify() passes an undeclared package by default, so this boundary is currently unchecked rather than checked and clean.
Entity com.mycompanyname.zero.identity.ou.OrganizationUnit lives in package com.mycompanyname.zero.identity.ou which has no package-info.java — Modulith's verify() passes an undeclared package by default, so this boundary is currently unchecked rather than checked and clean.
Entity com.mycompanyname.zero.identity.password.PasswordHistory lives in package com.mycompanyname.zero.identity.password which has no package-info.java — Modulith's verify() passes an undeclared package by default, so this boundary is currently unchecked rather than checked and clean.
Entity com.mycompanyname.zero.notification.domain.UserNotification lives in package com.mycompanyname.zero.notification.domain which has no package-info.java — Modulith's verify() passes an undeclared package by default, so this boundary is currently unchecked rather than checked and clean.
Entity com.mycompanyname.zero.saas.edition.Edition lives in package com.mycompanyname.zero.saas.edition which has no package-info.java — Modulith's verify() passes an undeclared package by default, so this boundary is currently unchecked rather than checked and clean.
Entity com.mycompanyname.zero.saas.edition.EditionFeature lives in package com.mycompanyname.zero.saas.edition which has no package-info.java — Modulith's verify() passes an undeclared package by default, so this boundary is currently unchecked rather than checked and clean.
Entity com.mycompanyname.zero.saas.feature.TenantFeature lives in package com.mycompanyname.zero.saas.feature which has no package-info.java — Modulith's verify() passes an undeclared package by default, so this boundary is currently unchecked rather than checked and clean.
Entity com.mycompanyname.zero.saas.subscription.Subscription lives in package com.mycompanyname.zero.saas.subscription which has no package-info.java — Modulith's verify() passes an undeclared package by default, so this boundary is currently unchecked rather than checked and clean.
Entity com.mycompanyname.zero.saas.subscription.SubscriptionEvent lives in package com.mycompanyname.zero.saas.subscription which has no package-info.java — Modulith's verify() passes an undeclared package by default, so this boundary is currently unchecked rather than checked and clean.
Entity com.mycompanyname.zero.settings.domain.Setting lives in package com.mycompanyname.zero.settings.domain which has no package-info.java — Modulith's verify() passes an undeclared package by default, so this boundary is currently unchecked rather than checked and clean.
