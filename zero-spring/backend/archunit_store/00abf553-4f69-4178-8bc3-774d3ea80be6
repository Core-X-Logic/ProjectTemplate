@EntityGraph combined with Pageable in com.mycompanyname.zero.identity.repo.RoleRepository.findAllByTenantId(java.lang.Long, org.springframework.data.domain.Pageable) — Hibernate cannot fetch a collection and paginate in one statement (HHH90003004); it loads every row and slices in memory. Split the query: page the ids, then fetch the associations for that page.
@EntityGraph combined with Pageable in com.mycompanyname.zero.identity.repo.RoleRepository.findAllByTenantIdIsNull(org.springframework.data.domain.Pageable) — Hibernate cannot fetch a collection and paginate in one statement (HHH90003004); it loads every row and slices in memory. Split the query: page the ids, then fetch the associations for that page.
@EntityGraph combined with Pageable in com.mycompanyname.zero.identity.repo.UserRepository.findAllByTenantId(java.lang.Long, org.springframework.data.domain.Pageable) — Hibernate cannot fetch a collection and paginate in one statement (HHH90003004); it loads every row and slices in memory. Split the query: page the ids, then fetch the associations for that page.
@EntityGraph combined with Pageable in com.mycompanyname.zero.identity.repo.UserRepository.findAllByTenantIdIsNull(org.springframework.data.domain.Pageable) — Hibernate cannot fetch a collection and paginate in one statement (HHH90003004); it loads every row and slices in memory. Split the query: page the ids, then fetch the associations for that page.
@EntityGraph combined with Pageable in com.mycompanyname.zero.identity.repo.UserRepository.searchByTenantId(java.lang.Long, java.lang.String, org.springframework.data.domain.Pageable) — Hibernate cannot fetch a collection and paginate in one statement (HHH90003004); it loads every row and slices in memory. Split the query: page the ids, then fetch the associations for that page.
@EntityGraph combined with Pageable in com.mycompanyname.zero.identity.repo.UserRepository.searchByTenantIdIsNull(java.lang.String, org.springframework.data.domain.Pageable) — Hibernate cannot fetch a collection and paginate in one statement (HHH90003004); it loads every row and slices in memory. Split the query: page the ids, then fetch the associations for that page.
