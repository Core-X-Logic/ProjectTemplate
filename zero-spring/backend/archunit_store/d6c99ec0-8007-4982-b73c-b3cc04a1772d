Unauthorized REST handler com.mycompanyname.zero.identity.auth.web.ImpersonationController.authenticate(com.mycompanyname.zero.identity.auth.web.dto.ImpersonateAuthRequest) — no @PreAuthorize on the method or its controller, so the endpoint is open to every authenticated caller of every tenant. Add @PreAuthorize, or add it to ArchitectureRules.INTENTIONALLY_ANONYMOUS with the matching SecurityConfig permitAll() entry.
Unauthorized REST handler com.mycompanyname.zero.identity.auth.web.ImpersonationController.backToImpersonator() — no @PreAuthorize on the method or its controller, so the endpoint is open to every authenticated caller of every tenant. Add @PreAuthorize, or add it to ArchitectureRules.INTENTIONALLY_ANONYMOUS with the matching SecurityConfig permitAll() entry.
Unauthorized REST handler com.mycompanyname.zero.identity.web.AuthController.logout(com.mycompanyname.zero.identity.web.dto.RefreshRequest) — no @PreAuthorize on the method or its controller, so the endpoint is open to every authenticated caller of every tenant. Add @PreAuthorize, or add it to ArchitectureRules.INTENTIONALLY_ANONYMOUS with the matching SecurityConfig permitAll() entry.
Unauthorized REST handler com.mycompanyname.zero.identity.web.AuthController.me() — no @PreAuthorize on the method or its controller, so the endpoint is open to every authenticated caller of every tenant. Add @PreAuthorize, or add it to ArchitectureRules.INTENTIONALLY_ANONYMOUS with the matching SecurityConfig permitAll() entry.
Unauthorized REST handler com.mycompanyname.zero.saas.subscription.web.SubscriptionController.me(org.springframework.security.oauth2.jwt.Jwt) — no @PreAuthorize on the method or its controller, so the endpoint is open to every authenticated caller of every tenant. Add @PreAuthorize, or add it to ArchitectureRules.INTENTIONALLY_ANONYMOUS with the matching SecurityConfig permitAll() entry.
Unauthorized REST handler com.mycompanyname.zero.settings.web.SettingController.clientSettings(org.springframework.security.oauth2.jwt.Jwt) — no @PreAuthorize on the method or its controller, so the endpoint is open to every authenticated caller of every tenant. Add @PreAuthorize, or add it to ArchitectureRules.INTENTIONALLY_ANONYMOUS with the matching SecurityConfig permitAll() entry.
