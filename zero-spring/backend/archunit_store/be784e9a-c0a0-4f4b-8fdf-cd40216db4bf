Tenant-scoped entity com.mycompanyname.zero.audit.domain.AuditLog has a tenant_id column but no Hibernate @Filter — cross-tenant reads will succeed silently and no positive test will notice. Add @Filter(name = "tenantFilter", condition = "tenant_id = :tenantId").
Tenant-scoped entity com.mycompanyname.zero.audit.domain.EntityChange has a tenant_id column but no Hibernate @Filter — cross-tenant reads will succeed silently and no positive test will notice. Add @Filter(name = "tenantFilter", condition = "tenant_id = :tenantId").
Tenant-scoped entity com.mycompanyname.zero.notification.domain.UserNotification has a tenant_id column but no Hibernate @Filter — cross-tenant reads will succeed silently and no positive test will notice. Add @Filter(name = "tenantFilter", condition = "tenant_id = :tenantId").
