Raw permission literal 'auditlogs.read' in @PreAuthorize on com.mycompanyname.zero.audit.web.AuditLogController.entityChanges(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'auditlogs.read' in @PreAuthorize on com.mycompanyname.zero.audit.web.AuditLogController.export(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'auditlogs.read' in @PreAuthorize on com.mycompanyname.zero.audit.web.AuditLogController.list(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'organizationunits.manage' in @PreAuthorize on com.mycompanyname.zero.identity.ou.web.OrganizationUnitController.create(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'organizationunits.manage' in @PreAuthorize on com.mycompanyname.zero.identity.ou.web.OrganizationUnitController.delete(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'organizationunits.manage' in @PreAuthorize on com.mycompanyname.zero.identity.ou.web.OrganizationUnitController.move(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'organizationunits.manage' in @PreAuthorize on com.mycompanyname.zero.identity.ou.web.OrganizationUnitController.tree(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'organizationunits.manage' in @PreAuthorize on com.mycompanyname.zero.identity.ou.web.OrganizationUnitController.update(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'roles.create' in @PreAuthorize on com.mycompanyname.zero.identity.web.RoleController.clone(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'roles.create' in @PreAuthorize on com.mycompanyname.zero.identity.web.RoleController.create(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'roles.delete' in @PreAuthorize on com.mycompanyname.zero.identity.web.RoleController.delete(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'roles.read' in @PreAuthorize on com.mycompanyname.zero.identity.web.RoleController.getById(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'roles.read' in @PreAuthorize on com.mycompanyname.zero.identity.web.RoleController.list(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'roles.update' in @PreAuthorize on com.mycompanyname.zero.identity.web.RoleController.update(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'settings.host.manage' in @PreAuthorize on com.mycompanyname.zero.settings.web.SettingController.hostSettings(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'settings.host.manage' in @PreAuthorize on com.mycompanyname.zero.settings.web.SettingController.updateHostSettings(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'settings.tenant.manage' in @PreAuthorize on com.mycompanyname.zero.settings.web.SettingController.tenantSettings(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'settings.tenant.manage' in @PreAuthorize on com.mycompanyname.zero.settings.web.SettingController.updateTenantSettings(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'tenants.manage' in @PreAuthorize on com.mycompanyname.zero.tenancy.web.TenantController.TenantController — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'users.create' in @PreAuthorize on com.mycompanyname.zero.identity.web.UserController.create(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'users.delete' in @PreAuthorize on com.mycompanyname.zero.identity.web.UserController.delete(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'users.impersonate' in @PreAuthorize on com.mycompanyname.zero.identity.auth.web.ImpersonationController.impersonate(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'users.read' in @PreAuthorize on com.mycompanyname.zero.identity.web.UserController.export(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'users.read' in @PreAuthorize on com.mycompanyname.zero.identity.web.UserController.getById(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'users.read' in @PreAuthorize on com.mycompanyname.zero.identity.web.UserController.list(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'users.unlock' in @PreAuthorize on com.mycompanyname.zero.identity.web.UserController.unlock(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'users.update' in @PreAuthorize on com.mycompanyname.zero.identity.web.UserController.activate(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'users.update' in @PreAuthorize on com.mycompanyname.zero.identity.web.UserController.assignOrganizationUnits(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'users.update' in @PreAuthorize on com.mycompanyname.zero.identity.web.UserController.assignRoles(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'users.update' in @PreAuthorize on com.mycompanyname.zero.identity.web.UserController.deactivate(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
Raw permission literal 'users.update' in @PreAuthorize on com.mycompanyname.zero.identity.web.UserController.update(..) — use the AppPermissions/SaasPermissions constant so a typo becomes a compile error instead of a permanent 403.
