package com.mycompanyname.zero.shared.domain;

public enum ErrorCode {
    NOT_FOUND,
    VALIDATION,
    UNAUTHORIZED,
    FORBIDDEN,
    CONFLICT,
    TENANT_UNKNOWN,
    LOGIN_FAILED,
    ACCOUNT_LOCKED,
    INTERNAL
}
