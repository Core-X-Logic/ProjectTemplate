# React 19 is the locked stack (FRONTEND-ARCHITECTURE.md §1), but
# react-helmet-async@2 still declares a react ^16||^17||^18 peer range even
# though it runs correctly on 19. Accept that stale peer so `npm ci`/`npm install`
# resolves deterministically instead of failing on the phantom conflict.
legacy-peer-deps=true
